# netlify.toml — tối giản & đúng mục tiêu
[build]
  publish = "."                     # <-- nếu index.html ở root repo
  functions = "netlify/functions"   # thư mục Functions của bạn

[functions]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "18"

# (tuỳ chọn) redirect cho /admin/* nếu bạn cần SPA:
# [[redirects]]
#   from = "/admin/*"
#   to = "/admin/index.html"
#   status = 200
